

// Runs at on Installation (initial)
chrome.runtime.onInstalled.addListener(function() {


  // There are a bunch of functions like this one that do various things,
  // We'll find them as we need them.
    // Saves some data to "chrome.storage" which is accessable by all files in extension
    // chrome.storage.sync.set({data: 'value'}, function() {
    //
    // });



  });
